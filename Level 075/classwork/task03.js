function traverseArr(arr) {
  for (let i = 0; i < arr.length; i++) {
    console.log(arr[i]);
  }
}

// ფუნქციის გამოძახება ორი განსხვავებული მასივით
traverseArr([1, 2, 3]);
traverseArr(["apple", "banana", "cherry"]);
