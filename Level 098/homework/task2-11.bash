# 2) Show files in current directory with details
ls -l

# 3) Clear the terminal
clear

# 4) Clear with shortcut (no command, just press Ctrl+L)

# 5) Go directly to home directory
cd

# 6) Navigate into Documents folder inside home
cd ~/Documents

# 7) Move one level up
cd ..

# 8) Create app.js and run it
echo 'console.log("Hello Node");' > app.js
node app.js

# 9) Display installed Node.js version
node -v

# 10) Make a directory named projects
mkdir projects

# 11) Create and delete test.txt
echo "test file" > test.txt
rm test.txt
